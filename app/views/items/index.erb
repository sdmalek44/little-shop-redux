<html>
  <head>
    <title>Items</title>
  </head>
  <body>
    <h1>Items</h1>
    <div>
      <a href="/items/new" class="new-item"><button class="new-item">Create A New Item</button></a>
    </div>
    <% if @items%>
      <% @items.each do |item| %>
      <img src="<%= item.image %>"><br>
      <a href="/items/<%= item.id %>" id='title-<%= item.id %>'><%= item.title %></a><br>
      <dib>$<%= item.money %></div><br>
      <% end %>
    <% end %>
  </body>
</html>
